import React from 'react';
import Layout from '../components/Layout';

const blogs = () => {
    return (
        <Layout>
            <h1>Blogs</h1>
        </Layout>
    );
};

export default blogs;